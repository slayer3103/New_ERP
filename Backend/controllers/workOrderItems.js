// const workOrderItemsModel = require('../models/workOrderItems');

// exports.createWorkOrderItems = (workOrderId, items, cb) => {
//   workOrderItemsModel.createItems(workOrderId, items, cb);
// };

// exports.getWorkOrderItems = (workOrderId, cb) => {
//   workOrderItemsModel.getItemsByWorkOrderId(workOrderId, cb);
// };

// exports.deleteWorkOrderItems = (workOrderId, cb) => {
//   workOrderItemsModel.deleteItemsByWorkOrderId(workOrderId, cb);
// };
